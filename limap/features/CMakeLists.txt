set(FOLDER_NAME "features")

LIMAP_ADD_SOURCES(
    featuremap.h featuremap.cc
    featurepatch.h featurepatch.cc

    dense_sift.h
    line_patch_extractor.h line_patch_extractor.cc

    bindings.cc
)

