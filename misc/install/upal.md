## UPAL installation

```bash
python -m pip install "upal-local-features@git+https://github.com/francois141/upal.git@62a022f"
```

The module ``upal`` should be available afterwards, and the checkpoint
downloads automatically on first use. UPAL runs on CPU but is only worth using
on a GPU.
